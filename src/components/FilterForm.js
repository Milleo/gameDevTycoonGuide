import { Card, Form } from "react-bootstrap";

const FilterForm = () => {
    return <Card>
        <Card.Header>Filter</Card.Header>
        <Form>
            
        </Form>
    </Card>
}

export default FilterForm;